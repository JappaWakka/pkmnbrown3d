version=2

:if:<Register.Registered(mettim)>=false
	@Text.Show(<system.token(Seashore_RivalHouse_Father_BeforeTim)>)
:else
	@Text.Show(<system.token(Seashore_RivalHouse_Father_AfterTim)>)
:endif
:end
