version=2

:if:<inventory.countitem(127)>=1
	@Text.Show(<system.token(FinalDungeon_SecurityDoors_WithCardKey)>)
	@entity.remove(1)
	@sound.play(SwitchDoor)
	@level.update
:else
	@Text.Show(<system.token(FinalDungeon_SecurityDoors_NoCardKey)>)
:endif
:end
