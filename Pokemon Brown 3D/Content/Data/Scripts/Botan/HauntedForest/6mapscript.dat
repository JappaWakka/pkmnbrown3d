version=2

:if:<Register.Registered(grunt0forest)>=True
@npc.remove(0)
:endif

:if:<Register.Registered(grunt1forest)>=True
@npc.remove(1)
:endif

:if:<Register.Registered(grunt2forest)>=True
@npc.remove(2)
:endif

:if:<Register.Registered(MrRumikoRescued)>=True
@npc.remove(3)
:endif
:end