version=2

@Text.Show(New here? We found one~of the birds.*Now we must find this one.)

:if:<Register.Registered(Lugia)>=True
	@screen.showpokemon(249,0,1)
:else
	@screen.showpokemon(250,0,1)
:endif
:end

