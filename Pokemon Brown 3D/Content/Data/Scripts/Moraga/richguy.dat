version=2

:if:<Register.Registered(coincase)>=false
@Text.Show(Hey kid, like the slots?*Well there's a casino in~Owsauri City*I'm stinkin rich so I can~just buy coins, so here.)
	@Item.Give(54,1)
	@Item.MessageGive(54,1)
	@Register.Register(coincase)

:else
	@Text.Show(Ha ha!*Win Master Balls with that!)
:endif
:end