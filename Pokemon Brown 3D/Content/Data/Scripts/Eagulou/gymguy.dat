version=2

:if:<Register.Registered(BeatGiovanni)>=false
	@Text.Show(<system.token(Eagulou_Gym_GymGuy_BeforeSilphGiovanni)>)
:else
	:if:<Register.Registered(starbadge_obtain)>=False
		@Text.Show(<system.token(Eagulou_Gym_GymGuy_BeforeBadge)>)
	:else
		@Text.Show(<system.token(Eagulou_Gym_GymGuy_AfterBadge)>)
	:endif
:endif
:end
