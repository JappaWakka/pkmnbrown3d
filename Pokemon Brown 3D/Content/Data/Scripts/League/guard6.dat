version=2

@Text.Show(You can pass here only if~you have the Psi Badge!)

:if:<Register.Registered(psibadge_obtain)>=True
	:if:<Register.Registered(guard6)>=False
		@Sound.Play(Success_Small)
		@Register.Register(guard6)
	:endif
	@Text.Show(Oh! That is the Psi Badge!~OK then! Please, go right~ahead!)
	
:endif

:if:<Register.Registered(psibadge_obtain)>=False
	@Text.Show(You don't have the Psi Badge~yet!*You have to have it to get~to the Pokémon League!)
:endif

:end
