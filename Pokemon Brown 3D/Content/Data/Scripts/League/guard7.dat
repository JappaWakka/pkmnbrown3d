version=2

:if:<Register.Registered(whitebadge_obtain)>=True <and> <Register.Registered(guard7)>=False
    @Text.Show(You can pass here only if~you have the White Badge!*Oh! That is the White Badge!~OK then! Please, go right~ahead!)
    @Register.Register(guard7)
:endif

:if:<Register.Registered(whitebadge_obtain)>=False <and> <Register.Registered(guard7)>=False
    @Text.Show(You can pass here only if~you have the White Badge!*You don't have the White~Badge yet!*You have to have it to get~to Pokémon League!)
    @player.turnto(2)
    @player.move(1)
:endif

:end
