version=2

@Text.Show(You can pass here only if~you have the White Badge!)

:if:<Register.Registered(whitebadge_obtain)>=True
	:if:<Register.Registered(guard7)>=False
		@Sound.Play(Success_Small)
		@Register.Register(guard7)
	:endif
	@Text.Show(Oh! That is the White Badge!~OK then! Please, go right~ahead!)
	
:endif

:if:<Register.Registered(whitebadge_obtain)>=False
	@Text.Show(You don't have the White~Badge yet!*You have to have it to get~to the Pokémon League!)
:endif

:end
