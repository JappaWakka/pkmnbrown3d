version=2

@Text.Show(You can pass here only if~you have the Sprout Badge!)

:if:<Register.Registered(sproutbadge_obtain)>=True
	:if:<Register.Registered(guard4)>=False
		@Sound.Play(Success_Small)
		@Register.Register(guard4)
	:endif
	@Text.Show(Oh! That is the Sprout Badge!~OK then! Please, go right~ahead!)
	
:endif

:if:<Register.Registered(sproutbadge_obtain)>=False
	@Text.Show(You don't have the Sprout~Badge yet!*You have to have it to get~to the Pokémon League!)
:endif

:end
