version=2

:if:<Register.Registered(sproutbadge_obtain)>=True <and> <Register.Registered(guard3)>=False
    @Text.Show(You can pass here only if~you have the Sprout Badge!*Oh! That is the Sprout Badge!~OK then! Please, go right~ahead!)
    @Register.Register(guard3)
:endif

:if:<Register.Registered(sproutbadge_obtain)>=False <and> <Register.Registered(guard3)>=False
    @Text.Show(You can pass here only if~you have the Sprout Badge!*You don't have the Sprout~Badge yet!*You have to have it to get~to Pokémon League!)
    @player.turnto(2)
    @player.move(1)
:endif

:end
