version=2
:if:<player.position(z)>=5 <and> <Register.Registered(guard1)>=False
	@NPC.Turn(0,3)
	@Player.TurnTo(1)
	@Text.Show(You can pass here only if~you have the Marine Badge!)
	
	:if:<Register.Registered(marinebadge_obtain)>=True
		:if:<Register.Registered(guard1)>=False
			@Sound.Play(Success_Small)
			@Register.Register(guard1)
		:endif
		@Text.Show(Oh! That is the Marine Badge!~OK then! Please, go right~ahead!)
		
	:endif
	
	:if:<Register.Registered(marinebadge_obtain)>=False
		@Text.Show(You don't have the Marine~Badge yet!*You have to have it to get~to Pokémon League!)
		@player.turnto(0)
		@player.move(1)
	:endif
:endif

:end
