version=2

@Text.Show(You can pass here only if~you have the Fist Badge!)

:if:<Register.Registered(fistbadge_obtain)>=True
	:if:<Register.Registered(guard5)>=False
		@Sound.Play(Success_Small)
		@Register.Register(guard5)
	:endif
	@Text.Show(Oh! That is the Fist Badge!~OK then! Please, go right~ahead!)
	
:endif

:if:<Register.Registered(fistbadge_obtain)>=False
	@Text.Show(You don't have the Fist Badge~yet!*You have to have it to get~to the Pokémon League!)
:endif

:end
