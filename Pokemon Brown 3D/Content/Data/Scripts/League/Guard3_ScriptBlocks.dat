version=2

:if:<player.position(z)>=121 <and> <Register.Registered(guard3)>=False
	@NPC.Turn(0,3)
	@Player.TurnTo(1)
	@Text.Show(You can pass here only if~you have the Sparky Badge!)
	
	:if:<Register.Registered(sparkybadge_obtain)>=True
		:if:<Register.Registered(guard3)>=False
			@Sound.Play(Success_Small)
			@Register.Register(guard3)
		:endif
		@Text.Show(Oh! That is the Sparky Badge!~OK then! Please, go right~ahead!)
		
	:endif
	
	:if:<Register.Registered(sparkybadge_obtain)>=False
		@Text.Show(You don't have the Sparky~Badge yet!*You have to have it to get~to the Pokémon League!)
		@player.turnto(2)
		@player.move(1)
	:endif
:endif
:end
